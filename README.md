# language_models
Playing around with language models large and small. Currently it has mistral-small-latest which, although it says small, is still has ~24B parameters. That's roughly 1/4th of the number of neurons in a human brain. Alongside mistral-small-latest, there is all-MiniLM-L6-v2 which is an embedding layer made up of ~22M parameters.
